# Save or Eliminate - Environment Variables

# Node environment (development or production)
NODE_ENV=production

# Server port (Railway assigns a random port via PORT env variable automatically)
# PORT=3001

# ===== RAILWAY DEPLOYMENT =====
# No additional configuration needed!
# Railway automatically:
# - Builds and deploys from github.com
# - Handles HTTPS/SSL
# - Manages PORT environment variable
# - Serves both server and client from same domain

