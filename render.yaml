services:
  - type: web
    name: save-or-eliminate-server
    runtime: node
    region: ohio
    plan: free
    buildCommand: cd server && npm install && npm run build
    startCommand: cd server && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
